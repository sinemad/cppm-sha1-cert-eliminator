# ClearPass server URL (include /api path)
CLEARPASS_SERVER=https://clearpass.example.com/api

# Authentication — use either API token OR client credentials
CLEARPASS_API_TOKEN=

CLEARPASS_CLIENT_ID=
CLEARPASS_CLIENT_SECRET=

# Set to "false" to disable SSL verification (not recommended for production)
CLEARPASS_VERIFY_SSL=true
